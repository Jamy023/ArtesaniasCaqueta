[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.xml", "php82Extensions.curl", "php82Packages.composer", "nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.build]
cmds = [
    "mkdir -p public/storage",
    "if [ -d storage/app/public ]; then cp -r storage/app/public/* public/storage/ || true; fi"
]

[phases.deploy]
cmds = ["echo 'Skipping storage link - using direct access'"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"